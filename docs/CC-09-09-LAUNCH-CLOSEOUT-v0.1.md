# EMVY CHECK — CC 09/09 LAUNCH CLOSEOUT v0.1

Status: FINAL PRE-IMAGE / PRE-LAUNCH CLOSEOUT BRIEF
Repo: `HerGotSystems/EMVYCHECK`
Target: 09/09/2026 public business launch

## Mission

Use the remaining CC time on high-value launch gaps only. Do not redesign the site and do not reopen architecture that already passed QA.

The previous hardening commit `1943963e05eb9392ed2c50aa120b597c6e5a8e64` is the baseline. Keep it unless a concrete bug is found.

Important correction: the live `https://emvycheck.com/` FormSubmit form is already activated and has successfully delivered messages. A localhost activation email generated by local QA is irrelevant and must not be treated as a production blocker.

## 1. Privacy notice completion — highest priority

Improve `privacy.html` from a useful short notice into a launch-usable privacy notice without legal theatre or invented facts.

Keep the existing plain-English style.

Add, where factually supportable from the current site/workflow:

- data-controller identity: EMVY CHECK, run by Michal Veltruský, contact `emvycheck@gmail.com`;
- categories of personal information actually received through the form/email;
- purposes;
- lawful-basis explanation appropriate to enquiries, steps requested before an order/contract, fulfilment of orders, and legal/accounting record obligations;
- categories of service providers currently involved in the public workflow: Cloudflare hosting, FormSubmit form delivery, Gmail/Google email, and AI service providers only when AI assistance is actually used on a message;
- retention wording based on necessity/criteria rather than inventing a fixed internal period if no verified period exists;
- the browser-only language preference stored by `language-help.js` in localStorage and the fact that it is not an advertising/tracking cookie;
- no automated commercial decision-making about customers;
- user rights in plain English, qualified with "where applicable";
- right to complain to the UK Information Commissioner's Office (ICO), with a link to the official ICO website;
- direct contact route for privacy questions.

Do not invent a postal address, company number, VAT number, DPO, retention period, international-transfer mechanism, analytics platform, cookie banner requirement, or legal registration status.

If a legal/privacy fact cannot be verified, phrase it narrowly or leave it out and report it as requiring Mike's decision.

Do not add a cookie banner: there is currently no advertising/analytics cookie system to consent to, and the language preference is functional local browser storage.

## 2. Make "Save Recipe" value obvious

The current product copy explains that a Recipe is editable/replayable, but this is a major practical value and should be clearer.

Add a short customer-facing explanation in the existing Canvas Grid guide / relevant homepage copy along these lines, without over-promising renderer compatibility:

> Save the Recipe as the lightweight rebuild file. It keeps the artwork setup so you can reopen and reproduce compatible work later, instead of storing every giant production image. Export the large production file when you actually need it.

Also update the multilingual quick-guide wording for `Save Recipe` consistently in EN/CS/HR/DE/FR/ES/PL.

Keep the important distinction:

- Recipe = rebuildable/editable setup;
- Preview = visual mock-up/screen output;
- Production export = large final production file;
- Recipe is not a commercial licence.

Do not change renderer, Art Code, export, or storage architecture.

## 3. Public discovery/share basics

Audit the current public site for basic launch metadata. The homepage currently has title/description but no verified canonical/Open Graph/social-share layer.

Add only safe, factual metadata:

- canonical URL on main public pages;
- Open Graph title/description/type/url;
- Twitter/X card title/description metadata;
- favicon/app icon references using existing repo icons where appropriate;
- a minimal `site.webmanifest` only if it is useful and does not pretend the site is an installable app it is not;
- `robots.txt` and `sitemap.xml` for the main public pages if absent.

Do NOT add `og:image` yet unless a real launch image already exists in the repo. We will choose the social/share hero from the real showcase artwork batch next.

Do not weaken the existing PHUL / no-AI-use metadata when adding search/share metadata. Search-engine discoverability and AI-training permission are separate concerns.

## 4. First-customer operational pack — internal docs only

Create practical internal templates under `docs/customer-ops/` so the first paying customer can be handled consistently without inventing checkout.

Create:

- `ORDER-CONFIRMATION-TEMPLATE-v0.1.md`
- `PERSONAL-USE-RIGHTS-GRANT-v0.1.md`
- `COMMERCIAL-USE-RIGHTS-GRANT-v0.1.md`
- `INVOICE-AND-DELIVERY-CHECKLIST-v0.1.md`

Base them strictly on the existing `FIRST-CUSTOMER-SALE-KIT-v0.1.md` and current manual-order model.

Requirements:

- plain English;
- one exact delivered artwork per rights grant;
- clear customer/project/order reference fields;
- permitted use;
- modification permission field;
- raw-file resale not permitted by default;
- sublicensing not permitted by default unless separately agreed;
- non-exclusive by default unless separately agreed;
- commercial grant must contain a specific scope field rather than vague "all business use";
- no Design Family rights;
- no automatic IP ownership transfer;
- no invented VAT/company/address/payment details;
- invoice checklist uses placeholders for the payment method and any legally required seller details Mike still needs to provide;
- customer approval / date evidence is retained;
- delivery checklist includes final files + written rights grant + invoice/payment record.

These files are INTERNAL templates, not public legal pages. Mark them clearly as operational launch templates and flag any field Mike must fill before use.

Do not turn these into automated licence issuance and do not touch D1, payments, subscriptions, or commercial-release code.

## 5. Production/live verification after deploy

After implementing the safe changes above, verify the actual public deployment rather than only localhost where possible.

Check:

- `https://emvycheck.com/`
- `/contact.html`
- `/privacy.html`
- `/art/`
- `/music/`
- `/thanks.html`
- Canvas Grid outbound link
- Instagram / Facebook / TikTok / YouTube outbound links

Verify live response headers include the launch security headers from `_headers`.

Do not submit another localhost FormSubmit activation test. If a form test is needed, test only the production form with an unmistakable QA message and do not change activation state.

## 6. Final freeze report

When complete, report only:

- exact files changed;
- tests performed;
- production URLs checked;
- whether the live security headers are present;
- any remaining launch blockers requiring Mike;
- any item deliberately left for the artwork-image pass.

## Constraints

- direct `main` is acceptable for these safe frontend/docs changes;
- no PR unless Mike explicitly asks;
- no `Co-Authored-By`;
- no Canvas Grid renderer logic changes;
- no D1/auth/payment/subscription/migration changes;
- no Paddle/Stripe checkout;
- no deployment architecture changes;
- no fake customers/testimonials/partners;
- no placeholder artwork presented as real work;
- do not touch the £1,000/m² public statement unless Mike explicitly changes it;
- keep Music working;
- mobile must remain clean.
